---
phase: 05-bug-fixes-new-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/js/app.js
  - resources/views/pages/home.blade.php
  - resources/views/partials/nav.blade.php
  - resources/views/partials/footer.blade.php
autonomous: true
requirements:
  - BUG-02
  - BUG-03

must_haves:
  truths:
    - "O ícone do LinkedIn exibe cor cinza neutra no estado padrão e muda para azul somente ao passar o mouse"
    - "Ao rolar a página para cima, os elementos fora da seção Hero animam novamente (mirror mode)"
    - "Os elementos da seção Hero (foto, h1, cargo, slogan, botões, scroll indicator) animam apenas uma vez (first visit)"
  artifacts:
    - path: "resources/js/app.js"
      provides: "AOS.init com mirror: true, once: false"
      contains: "mirror: true"
    - path: "resources/views/pages/home.blade.php"
      provides: "LinkedIn sem classe colored + data-aos-once nos elementos Hero"
      contains: "devicon-linkedin-plain text-2xl text-gray-400"
  key_links:
    - from: "resources/js/app.js"
      to: "AOS"
      via: "AOS.init({ mirror: true, once: false })"
      pattern: "mirror: true"
    - from: "resources/views/pages/home.blade.php"
      to: "Hero elements"
      via: "data-aos-once=\"true\" em 6 elementos"
      pattern: "data-aos-once=\"true\""
---

<objective>
Corrigir dois bugs visuais no portfólio: (1) o ícone do LinkedIn que exibe azul permanente por causa da classe `colored` e (2) a ausência de re-animação AOS ao rolar para cima, ativando o modo mirror com exceção seletiva na seção Hero.

Purpose: Estes são bugs visíveis a qualquer recrutador que visitar o site — o LinkedIn azul quebra a consistência visual e o AOS estático reduz o polish do portfólio.
Output: `app.js` com AOS mirror ativo, `home.blade.php` com LinkedIn corrigido e Hero com `data-aos-once="true"`. `nav.blade.php` e `footer.blade.php` inspecionados e corrigidos se tiverem o mesmo bug.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bug-fixes-new-skills/05-CONTEXT.md
@.planning/phases/05-bug-fixes-new-skills/05-UI-SPEC.md

<interfaces>
<!-- Contrato visual extraído do UI-SPEC.md e código existente -->

LinkedIn — estado atual (com bug):
```html
<i class="devicon-linkedin-plain colored text-2xl"></i>
```

LinkedIn — contrato corrigido (BUG-02, per D-03):
```html
<i class="devicon-linkedin-plain text-2xl text-gray-400 group-hover:text-accent transition-colors duration-300"></i>
```

AOS.init() — estado atual (com bug):
```js
AOS.init({
    duration: 700,
    once: true,
    offset: 80,
});
```

AOS.init() — contrato corrigido (BUG-03, per D-04, D-05):
```js
AOS.init({
    duration: 700,
    once: false,
    mirror: true,
    offset: 80,
});
```

Elementos Hero que precisam de data-aos-once="true" (per D-05, UI-SPEC linhas 163-169):
- div foto de perfil: data-aos="fade-down" → adicionar data-aos-once="true"
- h1 nome:           data-aos="fade-up" data-aos-delay="100" → adicionar data-aos-once="true"
- p cargo:           data-aos="fade-up" data-aos-delay="200" → adicionar data-aos-once="true"
- p slogan:          data-aos="fade-up" data-aos-delay="300" → adicionar data-aos-once="true"
- div botões CTA:    data-aos="fade-up" data-aos-delay="400" → adicionar data-aos-once="true"
- div scroll-indicator: data-aos="fade-up" data-aos-delay="600" → adicionar data-aos-once="true"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir AOS.init para mirror mode em app.js</name>
  <files>resources/js/app.js</files>
  <read_first>
    - resources/js/app.js — ler o arquivo completo antes de editar; a config AOS está nas linhas 15-19
  </read_first>
  <action>
    No arquivo `resources/js/app.js`, dentro do listener `DOMContentLoaded`, substituir o bloco `AOS.init({...})` existente:

    DE:
    ```js
    AOS.init({
        duration: 700,
        once: true,
        offset: 80,
    });
    ```

    PARA:
    ```js
    AOS.init({
        duration: 700,
        once: false,
        mirror: true,
        offset: 80,
    });
    ```

    Nenhuma outra linha do arquivo deve ser alterada. O Swiper, Alpine e imports permanecem intactos.
  </action>
  <verify>
    <automated>grep -n "mirror: true" resources/js/app.js && grep -n "once: false" resources/js/app.js</automated>
  </verify>
  <done>
    `resources/js/app.js` contém `mirror: true` e `once: false` no bloco AOS.init(). `once: true` não aparece mais no arquivo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Corrigir LinkedIn (home + nav + footer) + adicionar data-aos-once nos elementos Hero</name>
  <files>resources/views/pages/home.blade.php, resources/views/partials/nav.blade.php, resources/views/partials/footer.blade.php</files>
  <read_first>
    - resources/views/pages/home.blade.php — ler o arquivo completo; LinkedIn está na linha ~421, elementos Hero nas linhas 10-54
    - resources/views/partials/nav.blade.php — ler o arquivo completo; verificar se contém `devicon-linkedin-plain colored`
    - resources/views/partials/footer.blade.php — ler o arquivo completo; verificar se contém `devicon-linkedin-plain colored`
  </read_first>
  <action>
    **Alteração 1 — BUG-02: LinkedIn icon em home.blade.php (linha ~421)**

    DE:
    ```html
    <i class="devicon-linkedin-plain colored text-2xl"></i>
    ```

    PARA:
    ```html
    <i class="devicon-linkedin-plain text-2xl text-gray-400 group-hover:text-accent transition-colors duration-300"></i>
    ```

    A classe `colored` é removida (ela injeta estilos inline azul que sobrepõem qualquer Tailwind hover). As classes `text-gray-400 group-hover:text-accent transition-colors duration-300` replicam o padrão dos demais ícones sociais.

    **Alteração 2 — BUG-02: LinkedIn icon em nav.blade.php (se presente)**

    Ler `resources/views/partials/nav.blade.php`. Se contiver `devicon-linkedin-plain colored`, aplicar o mesmo fix:

    DE: `devicon-linkedin-plain colored`
    PARA: `devicon-linkedin-plain text-gray-400 group-hover:text-accent transition-colors duration-300`

    Adaptar as classes de tamanho (text-2xl ou outra) ao contexto do elemento na nav, preservando as demais classes do elemento.

    Se `devicon-linkedin-plain colored` NÃO existir em nav.blade.php, não tocar no arquivo.

    **Alteração 3 — BUG-02: LinkedIn icon em footer.blade.php (se presente)**

    Ler `resources/views/partials/footer.blade.php`. Se contiver `devicon-linkedin-plain colored`, aplicar o mesmo fix:

    DE: `devicon-linkedin-plain colored`
    PARA: `devicon-linkedin-plain text-gray-400 group-hover:text-accent transition-colors duration-300`

    Adaptar as classes de tamanho ao contexto do elemento no footer, preservando as demais classes.

    Se `devicon-linkedin-plain colored` NÃO existir em footer.blade.php, não tocar no arquivo.

    **Alteração 4 — BUG-03: data-aos-once nos 6 elementos Hero em home.blade.php**

    Nos seguintes elementos dentro de `<section id="hero">`, adicionar `data-aos-once="true"` como atributo:

    1. `<div class="mb-6" data-aos="fade-down">` →
       `<div class="mb-6" data-aos="fade-down" data-aos-once="true">`

    2. `<h1 ... data-aos="fade-up" data-aos-delay="100">` →
       `<h1 ... data-aos="fade-up" data-aos-delay="100" data-aos-once="true">`

    3. `<p class="text-xl ... data-aos="fade-up" data-aos-delay="200">` →
       `<p class="text-xl ... data-aos="fade-up" data-aos-delay="200" data-aos-once="true">`

    4. `<p class="text-gray-400 ... data-aos="fade-up" data-aos-delay="300">` →
       `<p class="text-gray-400 ... data-aos="fade-up" data-aos-delay="300" data-aos-once="true">`

    5. `<div class="flex flex-col ... data-aos="fade-up" data-aos-delay="400">` →
       `<div class="flex flex-col ... data-aos="fade-up" data-aos-delay="400" data-aos-once="true">`

    6. `<div class="absolute bottom-8 ... data-aos="fade-up" data-aos-delay="600">` →
       `<div class="absolute bottom-8 ... data-aos="fade-up" data-aos-delay="600" data-aos-once="true">`

    Todos os demais elementos `data-aos` fora da seção Hero (About, Skills, Projects, Contact) NÃO recebem `data-aos-once` — eles herdarão `mirror: true, once: false` do `AOS.init()`.
  </action>
  <verify>
    <automated>grep -c "data-aos-once=\"true\"" resources/views/pages/home.blade.php && grep "devicon-linkedin-plain" resources/views/pages/home.blade.php | grep -v "colored" && grep -rn "devicon-linkedin-plain colored" resources/views/partials/</automated>
  </verify>
  <done>
    - `home.blade.php` contém exatamente 6 ocorrências de `data-aos-once="true"`, todas dentro da section#hero.
    - A linha do LinkedIn em `home.blade.php` contém `devicon-linkedin-plain text-2xl text-gray-400 group-hover:text-accent` e NÃO contém a palavra `colored`.
    - O comando `grep -rn "devicon-linkedin-plain colored" resources/views/partials/` não retorna resultado (nav e footer corrigidos ou sem o bug).
  </done>
</task>

</tasks>

<verification>
Após as duas tasks:

```bash
# AOS mirror ativo
grep "mirror: true" resources/js/app.js
grep "once: false" resources/js/app.js

# LinkedIn sem colored em home
grep "devicon-linkedin-plain" resources/views/pages/home.blade.php
# Deve retornar linha com text-gray-400 e SEM "colored"

# LinkedIn sem colored em nav e footer
grep -rn "devicon-linkedin-plain colored" resources/views/partials/
# Deve retornar vazio (nenhum resultado)

# Exatamente 6 elementos Hero com once
grep -c "data-aos-once=\"true\"" resources/views/pages/home.blade.php
# Deve retornar: 6

# Build sem erros
npm run build
```
</verification>

<success_criteria>
1. `grep "mirror: true" resources/js/app.js` retorna resultado (linha existe)
2. `grep "once: false" resources/js/app.js` retorna resultado; `grep "once: true" resources/js/app.js` não retorna nada
3. `grep "devicon-linkedin-plain" resources/views/pages/home.blade.php` retorna linha com `text-gray-400` e sem `colored`
4. `grep -rn "devicon-linkedin-plain colored" resources/views/partials/` não retorna resultado
5. `grep -c "data-aos-once=\"true\"" resources/views/pages/home.blade.php` retorna `6`
6. `npm run build` finaliza sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-bug-fixes-new-skills/05-01-SUMMARY.md`
</output>
